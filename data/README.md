# Data Folder

Place the assignment CSV files here before running the SAS programs:

- `Dataset WBC.csv`
- `Dataset TWIN.csv`
- `Dataset THC.csv`

The scripts use these local project paths so the repository can be cloned and run without SAS Studio-specific absolute paths.

